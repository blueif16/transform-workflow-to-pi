# Vendor Repo Learning Scope

The learning track is scoped to `vendor/openclaw` and `vendor/hermes-agent`, with OpenClaw first. Future lessons should teach from local source files and keep domain language focused on these two repos instead of broad agent-framework theory.
