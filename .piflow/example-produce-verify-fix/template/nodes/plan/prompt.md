## Your task: turn the request into a concrete build plan

Read the request spec under `{{RUN}}` (the task/goal for this deliverable). Write a concrete build plan to
`plan/plan.md`: the steps to produce the deliverable AND an explicit, checkable acceptance bar (what "done and
correct" means) that the downstream verify node will judge against. Be specific enough that a producer can
follow it and a critic can grade the result — vague criteria let a wrong result pass.
