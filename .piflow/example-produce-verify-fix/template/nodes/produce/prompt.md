## Your task: implement the plan

Read the build plan at `{{RUN}}/plan/plan.md` and implement it, writing the resulting deliverable under `out/`
(your required artifact is `out/result.md`). Meet every step AND the plan's stated acceptance bar — the verify
node grades your output against that same bar and reroutes back to you on FAIL.

If you are re-entered after a FAIL, read the verify node's returned verdict and fix exactly what it flagged;
do not rewrite what already passed.
