Read the verified fragments under `frag/` and ASSEMBLE the final module to `out/module.md`.

The fragments passed the join gate, so compose them faithfully — stitch types · impl · tests into one coherent module, resolving only presentation/assembly details, never re-deciding the frozen spec.
