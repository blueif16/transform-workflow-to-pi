Read the frozen `spec/blueprint.json` and write the TYPES fragment — the module's type/interface surface — to `frag/types/types.md`.

Fill ONLY the types facet the spec assigns you; do not touch impl or tests. Match the interface the spec froze exactly so the other fragments compose against yours.
