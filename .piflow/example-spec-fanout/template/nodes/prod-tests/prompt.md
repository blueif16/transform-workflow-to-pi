Read the frozen `spec/blueprint.json` and write the TESTS fragment — the module's test suite — to `frag/tests/tests.md`.

Fill ONLY the tests facet the spec assigns you; do not touch types or impl. Cover the behavior the spec froze so the suite pins the same interface the other fragments build.
