Read the frozen `spec/blueprint.json` and write the IMPLEMENTATION fragment — the module's behavior/logic — to `frag/impl/impl.md`.

Fill ONLY the impl facet the spec assigns you; do not touch types or tests. Implement against the interface the spec froze so your fragment composes with the others.
