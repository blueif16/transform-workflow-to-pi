# Notes

- The root of this repo is now the teaching workspace.
- The first learning scope is the two vendored repos: `vendor/openclaw` and `vendor/hermes-agent`.
- Start lessons with OpenClaw, then use the same structure for Hermes.
- Keep domain modeling practical and local to these two repos for now.
- Prefer local files and official project docs over memory when creating lessons.
- Teach OpenClaw slice by slice. Keep each slice concise and maintain a compact domain model instead of a sprawling architecture essay.
