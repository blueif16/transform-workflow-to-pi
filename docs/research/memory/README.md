# Memory & self-correction — research home

> The design + evidence for piflow's **memory / self-correction (optimization) layer** — the piece the SDK
> migration left behind. We build the layer AND validate it on **game-omni** at the same time.

**`piflow-memory-v1.5.md` is the canonical source of truth.** It is the current plan AND the reference hub:
its **§8 (Grounding & provenance)** indexes every other file in this folder and says where each one lands. This
README is a pointer, not a second catalog — to avoid duplicate references, descriptions live in v1.5 (and in
each doc), never here.

- **To build:** `piflow-memory-v1.5.md` → §6 (the overlord/loop) + §7 (the first build: out-of-band Score+Triage).
- **The full corpus + what each file grounds:** `piflow-memory-v1.5.md` §8.
- **The substrate it builds on:** `piflow-memory-v1.md` (v1 — points forward to v1.5).

The canonical METHOD is the global `hermes-skill-system` skill; this folder is piflow's application of it
(law 5: reference, don't duplicate).
