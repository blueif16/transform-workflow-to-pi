---
title: "Installation"
summary: "Supported ways to install @piflow/core and the piflow CLI."
read_when:
  - You need the exact install commands
order: 3
draft: true
---

> Draft — fill in once the published install path (npm package name, global bin linking) is frozen.

## Requirements

- Node.js >= 20

## Install the SDK

```bash
# TODO: confirm published package name
npm install @piflow/core
```

## Install the CLI

```bash
# TODO: confirm the canonical global bin install
```
