{
  "id": "publish",
  "phase": "publish",
  "deps": ["draft"],
  "prompt": { "file": "prompt.md" },
  "tools": { "allow": ["read", "write", "submit_result"] },
  "contract": {
    "artifacts": ["spec/final.md"],
    "owns": ["spec/final.md"],
    "readScope": ["{{RUN}}"],
    "returnMode": "optional"
  }
}
